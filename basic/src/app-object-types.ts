{
  const fileName = "app-object-types.ts";
  console.log("Start ----->" + fileName);

  // const person: {
  //   name: string;
  //   age: number;
  // } = {
  const person = {
    name: "Maximilian",
    age: 30,
  };

  console.log(person.name);
}
